#pragma once

#include <string>

using namespace std;

class Controller
{
public:
    
    static void Solve(const string& filePath);
    static void SolveAllInDirectory(const string& dirPath);

private:

};